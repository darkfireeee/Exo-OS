[package]
name = "exo-nvme"
version = "0.1.0"
edition = "2021"
license.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
spin = { workspace = true }
log = { workspace = true }

[features]
default = []
