//! Exo-OS Kernel Main Entry Point
//! 
//! Ce fichier est le point d'entrée du binaire kernel.
//! Il réexporte simplement la fonction rust_main de la lib.

#![no_std]
#![no_main]

extern crate exo_kernel;

/// Point d'entrée Rust appelé depuis boot.c
/// Note: _start est défini dans boot.asm, pas ici
#[no_mangle]
pub extern "C" fn rust_kernel_entry(magic: u32, multiboot_info: u64) -> ! {
    // Appeler la fonction principale de la bibliothèque kernel
    exo_kernel::rust_main(magic, multiboot_info)
}
